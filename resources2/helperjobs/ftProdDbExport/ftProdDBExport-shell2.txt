echo $EXTRACT_VERSION
echo ${EXTRACT_VERSION}

echo "Downloading Database-$EXTRACT_VERSION.zip"
wget --quiet --no-check-certificate "http://${NEXUS}/repository/public/gov/dhs/uscis/elis2/Database/${EXTRACT_VERSION}/Database-${EXTRACT_VERSION}.zip" -O Database-${EXTRACT_VERSION}.zip

unzip -qu ${WORKSPACE}/Database-$EXTRACT_VERSION.zip -d ${WORKSPACE}/Database-$EXTRACT_VERSION

echo "Copying gradle.properties"

cp ${WORKSPACE}/ft/gradle.properties ${WORKSPACE}/Database-$EXTRACT_VERSION

ssh -t ec2-user@10.193.129.162 -o StrictHostKeyChecking=no "ls -lart; cd /tmp ; ls -lart;  ./newProdPrepare.sh"
